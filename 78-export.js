const veri = (data) =>{
    return data.charAt(0).toUpperCase();
}

export { veri };              // veri fonksiyonunu baska projeler icin disari aktardik
export const foo = "bar";     // ve ayni sekilde degiskenleride aktarabiliriz.
export const bar = "foo";